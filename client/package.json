{
  "name": "client",
  "version": "0.0.1",
  "description": "Front-end for the app",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "./node_modules/.bin/browserify src/main.js -t babelify --outfile build/main.js",
    "watch": "./node_modules/.bin/watchify src/main.js -d -v -t babelify -o build/main.js"
  },
  "main": "client-server.js",
  "author": "MW",
  "devDependencies": {
    "babelify": "^6.1.3",
    "browserify": "^11.0.1",
    "express": "^4.13.3",
    "http-server": "^0.8.0",
    "react-tools": "^0.13.3",
    "stylus": "^0.52.0",
    "watchify": "^3.3.1"
  },
  "dependencies": {
    "flux": "^2.0.3",
    "localforage": "^1.2.7",
    "moment": "^2.10.6",
    "moment-timezone": "^0.4.0",
    "react": "^0.13.3",
    "react-router": "^0.13.3",
    "reqwest": "^2.0.1",
    "xhr2": "^0.1.3"
  }
}
